import { KeywordHighlightPipe } from './keyword-highlight-pipe';

describe('KeywordHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new KeywordHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
