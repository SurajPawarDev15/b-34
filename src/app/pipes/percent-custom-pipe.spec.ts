import { PercentCustomPipe } from './percent-custom-pipe';

describe('PercentCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
